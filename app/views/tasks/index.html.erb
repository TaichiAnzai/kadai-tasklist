<h1>タスク一覧</h1>

<ul>
  <% @tasks.each do |task| %>
    <li><%= task.id %> : <%= link_to task.content, task %> , 投稿日時：<%= task.created_at %> , 最終更新日時：<%= task.updated_at %></li>
  <% end %>
</ul>

<%= link_to '新規タスクの投稿', new_task_path %>